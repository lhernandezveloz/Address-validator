{
  "name": "address-validator-cli",
  "version": "1.0.0",
  "description": "This is an app to validate address typed by the user or passed in .cvs file",
  "main": "index.js",
  "scripts": {
    "start": "node -r dotenv/config index.js",
    "test": "jest"
  },
  "author": "leonal Hernandez",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.1",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}
